{
  "name": "Gesso",
  "type": "{{ mode }}",
  "colors": {
    "editor.background": "{{ background }}",
    "editor.foreground": "{{ foreground }}",
    "focusBorder": "{{ accent }}",
    "selection.background": "{{ selection }}"
  }
}
